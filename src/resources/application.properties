spring.application.name=PoisePMS

# Database configuration (uses environment variables on Render)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/PoisePMS}
spring.datasource.username=${DB_USERNAME:richard}
spring.datasource.password=${DB_PASSWORD:richard}

# Server configuration
server.port=${PORT:8080}